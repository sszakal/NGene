namespace OnesCounting
{
    class Program
    {
        static void Main(string[] args)
        {
            //Engine engine = Engine
            //    .Builder(OnesCounting::count, BinaryChromosome.Of(20, 0.15D), new Chromosome[0]).populationSize(500)
            //    .Selector(new RouletteWheelSelector()).alterers(new Mutator(0.55D), new Alterer[] { new SinglePointCrossover(0.06D) }).build();
            //EvolutionStatistics statistics = EvolutionStatistics.ofNumber();
            //Phenotype best = (Phenotype)engine.stream().limit(limit.bySteadyFitness(7)).limit(100L).peek(statistics).collect(EvolutionResult.toBestPhenotype());
            //System.out.println(statistics);
            //System.out.println(best);
        }
    }
}
